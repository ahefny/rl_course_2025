# Python dependencies for the Connect-4 demos in this directory.
# Install with:  pip install -r requirements.txt
#
# NOTE: the `graphviz` package below is only a Python wrapper. Rendering the
# MCTS search tree also needs the Graphviz system binary (`dot`), installed via
# your OS package manager, e.g.:  sudo apt install graphviz

numpy>=1.24,<3
gradio>=6.0,<7
graphviz>=0.20,<1
torch>=2.0
